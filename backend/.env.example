# PPG Glucose Prediction System - Backend Configuration

# Database settings for local PostgreSQL (without Docker)
DB_USER=postgres
DB_PASSWORD=password123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ppg_glucose_db

# Or use a full connection string (overrides the above parameters if set)
# DATABASE_URL=postgresql+psycopg2://postgres:password123@localhost:5432/ppg_glucose_db

# Project name
PROJECT_NAME="PPG Glucose Prediction System"
